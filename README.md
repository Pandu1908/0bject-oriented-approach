# 0bject-oriented-approach
A Python program demonstrating the object-oriented approach by defining a Student class with instance attributes, creating Student objects, storing them in a roster, and accessing object attributes. It also uses a list comprehension to filter objects based on the is_hosteler attribute and display their names.
